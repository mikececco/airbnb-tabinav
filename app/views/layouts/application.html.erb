<!DOCTYPE html>
<html>
  <head>
    <title>AirbnbTabinav</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "shared/navbar" unless ["devise/sessions", "devise/registrations"].include? params[:controller] %>
    <%= yield %>
    <%= render "shared/footer" unless ["devise/sessions", "devise/registrations"].include? params[:controller] %>
  </body>
</html>
